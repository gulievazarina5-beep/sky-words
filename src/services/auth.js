export const loginUser = async ({ login, password }) => {const response = await fetch('https://wedev-api.sky.pro/api/user/login', {method: 'POST',body: JSON.stringify({ login, password }),});if (!response.ok) {const errorData = await response.json().catch(() => ({}));throw new Error(errorData.error || 'Неверный логин или пароль');}return await response.json();};